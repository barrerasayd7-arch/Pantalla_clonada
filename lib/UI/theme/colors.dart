import 'package:flutter/material.dart';

// Colores centrales de la app, intente que fueran lo mas similar a BetPlay.
class AppColors {
  static const Color fondo = Color(0xFF0B1739);      // azul oscuro de fondo
  static const Color fondoTarjeta = Color(0xFF122057); // azul un poco mas claro (tarjetas)
  static const Color verde = Color(0xFF14A44D);        // verde principal (botones)
  static const Color verdeClaro = Color(0xFFB7F7C6);   // verde clarito (boton Crear Cuenta)
  static const Color texto = Colors.white;
  static const Color textoSecundario = Color(0xFF8C9BC4);
}
